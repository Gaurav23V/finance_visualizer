# Example environment variables for Finance Visualizer
# Copy to .env.local (development) or .env.production (production)

# MongoDB connection string (replace with your own)
MONGODB_URI=mongodb+srv://username:password@cluster0.mongodb.net/finance_visualizer?retryWrites=true&w=majority 